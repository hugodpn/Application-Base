<h1><%= t('categories.edit_category') %></h1>

<% form_for([:admin, @category]) do |f| %>
  <%= f.error_messages %>


  <%= render :partial => "form", :locals => { :f => f } %>

  <p>
    <%= f.submit t('general.update') %>
  </p>
<% end %>
